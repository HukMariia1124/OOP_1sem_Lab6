<Window x:Class="WpfImageMirror.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Image Mirroring" Height="450" Width="600"
        WindowStartupLocation="CenterScreen">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <GroupBox Header="Налаштування" Grid.Row="0" Padding="5">
            <StackPanel>
                <TextBlock Text="Шлях до папки з зображеннями:" Margin="0,0,0,5"/>
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>
                    <TextBox x:Name="TxtFolderPath" Text="." VerticalAlignment="Center" Padding="3"/>
                    <Button Content="Скинути шлях" Grid.Column="1" Margin="5,0,0,0" Padding="10,2" Click="BtnRefresh_Click"/>
                </Grid>
                <TextBlock Text="Введіть повний шлях або залиште '.' для поточної папки програми" 
                           FontSize="10" Foreground="Gray" Margin="0,2,0,0"/>
            </StackPanel>
        </GroupBox>

        <Button x:Name="BtnStart" Content="Почати обробку (Дзеркальне відображення)" 
                Grid.Row="1" Margin="0,10" Height="30" Click="BtnStart_Click" 
                FontWeight="Bold" Background="#DDDDDD"/>

        <StackPanel Grid.Row="2" Margin="0,0,0,10">
            <TextBlock x:Name="TxtStatus" Text="Готово до роботи" Margin="0,0,0,5"/>
            <ProgressBar x:Name="PbStatus" Height="20" Minimum="0" Maximum="100"/>
        </StackPanel>

        <GroupBox Header="Журнал подій" Grid.Row="3">
            <ListBox x:Name="LstLog" FontFamily="Consolas" FontSize="12"/>
        </GroupBox>
    </Grid>
</Window>